import { Router } from 'express';
import * as QuestionController from '../controllers/question.controller.js';

const router = Router();

// router.get('/example', QuestionController.method);

export default router;
