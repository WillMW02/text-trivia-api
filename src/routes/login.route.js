import { Router } from 'express';
import * as LoginController from '../controllers/login.controller.js';

const router = Router();

// router.get('/example', LoginController.method);

export default router;
